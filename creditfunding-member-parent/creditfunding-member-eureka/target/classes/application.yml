server:
  port: 1000
spring:
  application:
    name: itxiaodu-credit-eureka
#  cloud:
#    inetutils:
#      timeout-seconds: 10
eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/